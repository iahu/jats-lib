include:
  - project: hushuibin/ci_templates
    ref: master
    file:
      - yarn.yaml

stages:
  - build
  - deploy

build:
  stage: build
  extends: .yarn:build
  only:
    - tags
  tags:
    - zt
    - docker

deploy:
  stage: deploy
  only:
    - tags
  script:
    - npm publish
